apiVersion: v2
name: postgres
description: Chart proprio pro Postgres do lab - simples, sem HA, pensado pra ambiente de estudo (nao usar em producao de verdade sem revisar persistencia e secrets)
type: application
version: 0.1.0
appVersion: "17"
